---
name: kline-html-report
description: 单标的 K 线 × 事件对齐复盘的自包含交互 HTML——K线/成交量/均线、可筛选事件标注与影响评级、规则触发的变化点标记、事件后收益、全链路溯源面板
kind: html
blocks: heading, narrative, table, kline_chart, changepoint_table
---

# K 线事件复盘报告方法论

## 产物定位

给"想验证结论"的读者：每个事件能点回原始来源，每个变化点能看到触发规则与数据窗口，
断网双击可开（Plotly 与数据全部内嵌，零外部请求）。

## 结构组织建议（按任务裁剪，不是固定模板）

1. 开篇 narrative：研究问题、数据区间、核心结论（2–4 句，克制、可验证）；
2. kline_chart：主图，必须且只能一个——事件标注（events）与变化点（changepoints）
   都挂在这里；
3. 对齐分析章节（heading + narrative 若干）：按时间段或主题组织，
   每段声明引用的 evidence_refs；
4. changepoint_table：变化点明细，让"拐点从哪来"可查；
5. 收尾 narrative：局限性与未尽事项（数据修订风险、未解释的变化点等）。

## 事件标注要求

- `impact` 1–5：5=直接改变盈利/估值中枢或触发大幅重定价；4=直接影响需求、
  产品周期、政策约束；3=影响生态与中期预期；1–2=观察级；
- `direction`：up/down/mixed/neutral，指事件对标的的方向性含义，不是当日涨跌；
- `move`：一句话描述与该事件吻合的行情变化（拐点/加速/回撤/趋势确认…）；
- 每个事件至少 1 个 `sources`（可点击原文），并挂 `evidence_refs` 回链检索记录；
- 事件必须与某个变化点时间窗吻合，或明确标注为背景事件；
  **找不到对应事件的变化点保留在 changepoint_table，不强行归因。**

## 叙事写作要求

- 用"时间吻合 + 影响逻辑"的措辞（"X 后 N 个交易日内出现…"），不用因果断言；
- 数字给来源：涉及涨跌幅、区间收益的句子挂 evidence_refs；
- 诚实原则：数据缺口、检索无果、评级依据不足要写出来。
